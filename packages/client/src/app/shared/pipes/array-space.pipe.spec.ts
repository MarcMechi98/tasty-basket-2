import { ArraySpacePipe } from './array-space.pipe';

describe('ArraySpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ArraySpacePipe();
    expect(pipe).toBeTruthy();
  });
});
